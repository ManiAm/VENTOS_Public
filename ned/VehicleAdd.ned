
package c3po.ned;


simple VehicleAdd
{
    parameters:
        @class(VehicleAdd);
        @display("i=block/network2");
        
        bool on = default(true);
        int mode;
        int totalVehicles;
        int lambda;
}

